import React from 'react';
import { LoginContainer } from './styles';

type Props = {};

const Login = (props: Props) => {
  return (
    <LoginContainer>
      Login
    </LoginContainer>
  );
}

export default Login;
